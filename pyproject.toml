[build-system]
requires = [
    "hatchling",
    "hatch-rs>=0.1.4",
]
build-backend = "hatchling.build"

[project]
name = "fsspec-rs"
authors = [
    {name = "the fsspec-rs authors", email = "dev@1kbgz.com"},
]
description = "fsspec-compatible backends with Rust accleration"
readme = "README.md"
license = { text = "Apache-2.0" }
version = "0.1.1"
requires-python = ">=3.10"
keywords = []

classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Rust",
    "Programming Language :: Python",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

dependencies = [
    "fsspec",
]

[project.optional-dependencies]
develop = [
    "asv",
    "build",
    "bump-my-version",
    "check-dist",
    "cibuildwheel",
    "codespell",
    "hatch-rs",
    "hatchling",
    "laxate",
    "mdformat",
    "mdformat-tables>=1",
    "pytest",
    "pytest-benchmark",
    "pytest-cov",
    "ruff",
    "s3fs",
    "twine",
    "ty",
    "uv",
    "wheel",
]

[project.entry-points."fsspec.specs"]
file-rs = "fsspec_rs.local:LocalFileSystem"
local-rs = "fsspec_rs.local:LocalFileSystem"
s3-rs = "fsspec_rs.s3:S3FileSystem"

[project.scripts]

[project.urls]
Repository = "https://github.com/1kbgz/fsspec-rs"
Homepage = "https://github.com/1kbgz/fsspec-rs"

[tool.bumpversion]
current_version = "0.1.1"
commit = true
tag = true
commit_args = "-s"

[[tool.bumpversion.files]]
filename = "fsspec_rs/__init__.py"
search = '__version__ = "{current_version}"'
replace = '__version__ = "{new_version}"'

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[[tool.bumpversion.files]]
filename = "Cargo.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[[tool.bumpversion.files]]
filename = "rust/Cargo.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[tool.cibuildwheel]
build = "cp310-*"
test-command = "pytest -vvv {project}/fsspec_rs/tests"
test-extras = "develop"

[tool.cibuildwheel.linux]
before-all = """
curl https://sh.rustup.rs -sSf | sh -s -- --default-toolchain=stable --profile=minimal -y
rustup target add aarch64-unknown-linux-gnu
rustup target add x86_64-unknown-linux-gnu
rustup show
"""
environment = {PATH="$HOME/.cargo/bin:$PATH", CARGO_TERM_COLOR="always"}
skip = "*i686* *musllinux*"

[tool.cibuildwheel.macos]
environment = {PATH="$HOME/.cargo/bin:$PATH", CARGO_TERM_COLOR="always", MACOS_DEPLOYMENT_TARGET=11.0}
archs = "arm64"

[tool.cibuildwheel.windows]
environment = {PATH="$UserProfile\\.cargo\bin;$PATH", CARGO_TERM_COLOR="always"}
skip = "*win32 *arm_64"

[tool.coverage.run]
branch = true
omit = [
    "fsspec_rs/tests/integration/",
]

[tool.coverage.report]
exclude_also = [
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@(abc\\.)?abstractmethod",
]
ignore_errors = true
fail_under = 50

[tool.hatch.build]
artifacts = [
    "project/*.dll",
    "project/*.dylib",
    "project/*.so",
]

[tool.hatch.build.sources]
src = "/"

[tool.hatch.build.hooks.hatch-rs]
verbose = true
abi3 = true
module = "fsspec_rs"
path = "."

[tool.hatch.build.targets.sdist]
packages = [
    "fsspec_rs",
    "rust",
    "Cargo.toml",
    "Cargo.lock",
]
exclude = [
    "rust/Makefile",
    "target",
]

[tool.hatch.build.targets.wheel]
packages = [
    "fsspec_rs",
]
exclude = [
    "rust",
]

[tool.pytest.ini_options]
addopts = [
    "-vvv",
    "--junitxml=junit.xml",
]
testpaths = "fsspec_rs/tests"

[tool.ruff]
line-length = 150

[tool.ruff.lint]
extend-select = [
    "I",
]

[tool.ruff.lint.isort]
combine-as-imports = true
default-section = "third-party"
known-first-party = [
    "fsspec_rs",
]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",
    "F403",
]

[tool.yardang]
title = "fsspec-rs"
root = "README.md"
pages = []
use-autoapi = false
